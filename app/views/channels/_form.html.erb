<%= form_with model: channel do |form| %>

  <% if channel.errors.any? %>
    <div style="background:rgba(239,83,80,0.08); border:1px solid rgba(239,83,80,0.2); border-radius:8px; padding:1rem; margin-bottom:1.25rem; font-size:0.875rem; color:#ef5350;">
      <div style="margin-bottom:0.5rem;"><%= pluralize(channel.errors.count, "error") %> prevented this channel from being saved:</div>
      <ul style="margin:0; padding-left:1.25rem;">
        <% channel.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-4">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_area :description, class: "form-control", rows: 3 %>
  </div>

  <div>
    <%= form.submit class: "vn-btn-primary" %>
  </div>

<% end %>